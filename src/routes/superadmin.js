const express = require('express');

const router = express.Router();

// SuperAdmin routes (if any other routes needed in future)
// Login routes have been moved to /api/auth

module.exports = router;

